#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	int N;
	float a, b;
	int cnt = 1;
	cin >> N >> a >> b;

	while (1) {
		a = ceil(a / 2);
		b = ceil(b / 2);
		if (a == b) {
			cout << cnt;
			return 0;
		}
		cnt++;
	}

}
