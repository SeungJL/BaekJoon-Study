#include <iostream>
#include <cmath>

using namespace std;

int main() {
    unsigned long long n;
    unsigned long long m;
    cin >> n;
    m = sqrt(n);
    if (pow(m, 2) == n) {
        cout << m;
    }
    else {
        cout << m + 1;
    }
}
